<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Msg">

	<insert id="insertSendMsg" parameterType="hashMap" statementType="STATEMENT" useGeneratedKeys="true" keyProperty="sendMsgNo">
	
	INSERT INTO send_msg(writerId, sendMsgContent)
	VALUES(#{writerId}, #{sendMsgContent})
	
	</insert>
	
</mapper>